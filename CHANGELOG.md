# 更新日志

## 2026-05-24

- 移除 Windows 版，仓库改成纯 macOS 项目；README、`.gitignore` 同步清理。
- 看屏幕改成两段式：OCR 文字先经本地大模型脱敏，删掉敏感信息后才发往云端 DeepSeek。
- 本地模型连不上时不再把原始屏幕文字发往云端（fail-closed）；脱敏后没有可安全分享的内容也会跳过这一轮。
- 新增本地模型配置：菜单 **设置本地模型** / **查看本地模型**（快捷键 `⌘G`），默认走 Ollama 的 OpenAI 兼容接口（`qwen2.5:3b`）。
- DeepSeek API Key 改用 macOS Security 框架直接读写钥匙串，不再把 key 作为命令行参数传给 `/usr/bin/security`。

## 2026-05-21

- 加入本机记忆：熊可以记录用户偏好，并在聊天时参考。
- 加入自定义性格：可以自己写熊的人设和说话方式。
- 加入简单计时：到点后熊会弹出提醒。
- 更新中文 README：补充记忆、性格、计时和快捷键说明。
- 补充环境要求：说明 macOS、AI 对话、屏幕权限和透明效果。
- macOS 构建时如果没有自定义 `.icns`，会自动从熊 GIF 生成 app 图标。
- 调整熊的问候逻辑：只在打开聊天时先问好，后续回复和提醒不再每次重复固定开场白。

## 2026-05-22

- 修复 GIF 载入逻辑：`assets/` 里放多少 `.gif` 都会按文件名顺序轮播，不再要求固定文件名。
- macOS 构建脚本会把 `macos/assets/` 里的所有 GIF 打包进 `.app`。
- 调整默认性格：熊更温暖可爱，更像一只觉得自己领养了人的小熊。
- README 增加旧版本最小更新说明，提醒用户只替换代码文件并保留自己的 GIF 和图标。
